<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Dashboard Phụ Huynh - SWF Math' %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/student-pages.css">
    <link rel="stylesheet" href="/css/item-card.css">
    <style>
        .brand-role {
            font-size: 0.75rem;
            color: #667eea;
            background: rgba(102, 126, 234, 0.1);
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-weight: 700;
            margin-left: 0.5rem;
        }
    </style>
</head>
<body>
    <!-- Dashboard Header -->
    <header class="dashboard-header">
        <div class="header-left">
            <button class="sidebar-toggle" id="sidebarToggle">☰</button>
            <div class="brand">
                <img src="/img/logo.svg" alt="SWF Math" class="brand-logo">
                <span class="brand-name">SWF Math</span>
                <span class="brand-role">Phụ Huynh</span>
            </div>
        </div>
        
        <div class="header-right">
            <div class="search-box">
                <input type="text" placeholder="Tìm kiếm...">
                <span class="search-icon">🔍</span>
            </div>
            <button class="notification-btn">
                🔔
                <span class="badge">2</span>
            </button>
            
            <!-- User Dropdown -->
            <%- include('../partials/user-dropdown', { user: user }) %>
        </div>
    </header>

    <div class="dashboard-container">
        <!-- Sidebar -->
        <aside class="sidebar" id="sidebar">
            <nav class="sidebar-nav">
                <a href="/parent/dashboard" class="nav-item <%= currentPath === '/parent/dashboard' ? 'active' : '' %>">
                    <span class="nav-icon">🏠</span>
                    <span class="nav-text">Trang Chủ</span>
                </a>
                <a href="/parent/reports" class="nav-item <%= currentPath === '/parent/reports' ? 'active' : '' %>">
                    <span class="nav-icon">📊</span>
                    <span class="nav-text">Báo Cáo</span>
                </a>
                <a href="/settings" class="nav-item <%= currentPath === '/settings' ? 'active' : '' %>">
                    <span class="nav-icon">⚙️</span>
                    <span class="nav-text">Cài Đặt</span>
                </a>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <%- body %>
        </main>
    </div>

    <script src="/js/main.js"></script>
    <script src="/js/dashboard.js"></script>
</body>
</html>
