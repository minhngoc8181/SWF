<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Dashboard - SWF Math' %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/student-pages.css">
    <link rel="stylesheet" href="/css/item-card.css">
</head>
<body>
    <!-- Dashboard Header -->
    <header class="dashboard-header">
        <div class="header-left">
            <button class="sidebar-toggle" id="sidebarToggle">☰</button>
            <div class="brand">
                <img src="/img/logo.svg" alt="SWF Math" class="brand-logo">
                <span class="brand-name">SWF Math</span>
            </div>
        </div>
        
        <div class="header-right">
            <div class="search-box">
                <input type="text" placeholder="Tìm kiếm...">
                <span class="search-icon">🔍</span>
            </div>
            <button class="notification-btn">
                🔔
                <span class="badge">3</span>
            </button>
            
            <!-- User Dropdown -->
            <%- include('../partials/user-dropdown', { user: user }) %>
        </div>
    </header>

    <div class="dashboard-container">
        <!-- Sidebar -->
        <aside class="sidebar" id="sidebar">
            <nav class="sidebar-nav">
                <a href="/student/dashboard" class="nav-item <%= currentPath === '/student/dashboard' ? 'active' : '' %>">
                    <span class="nav-icon">🏠</span>
                    <span class="nav-text">Trang Chủ</span>
                </a>
                <a href="/student/tests" class="nav-item <%= currentPath.startsWith('/student/test') ? 'active' : '' %>">
                    <span class="nav-icon">📝</span>
                    <span class="nav-text">Bài Thi</span>
                </a>
                <a href="/student/courses" class="nav-item <%= currentPath.startsWith('/student/course') ? 'active' : '' %>">
                    <span class="nav-icon">📚</span>
                    <span class="nav-text">Khóa Học</span>
                </a>
                <a href="/student/games" class="nav-item <%= currentPath === '/student/games' ? 'active' : '' %>">
                    <span class="nav-icon">🎮</span>
                    <span class="nav-text">Trò Chơi</span>
                </a>
                <a href="/student/progress" class="nav-item <%= currentPath === '/student/progress' ? 'active' : '' %>">
                    <span class="nav-icon">📊</span>
                    <span class="nav-text">Tiến Độ</span>
                </a>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <%- body %>
        </main>
    </div>

    <script src="/js/main.js"></script>
    <script src="/js/dashboard.js"></script>
</body>
</html>
