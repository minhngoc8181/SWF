<!-- Page Header -->
<div class="page-header">
    <h1>⚙️ Cài Đặt</h1>
    <p>Quản lý thông tin tài khoản và tùy chỉnh hệ thống</p>
</div>

<div class="settings-container">
    <!-- Account Settings -->
    <div class="settings-section">
        <div class="section-title">
            <span class="title-icon">👤</span>
            <h2>Thông Tin Tài Khoản</h2>
        </div>
        
        <div class="settings-card">
            <div class="form-group">
                <label>Họ và Tên</label>
                <input type="text" class="form-input" value="<%= user.name %>" placeholder="Nhập họ tên">
            </div>
            
            <div class="form-group">
                <label>Email</label>
                <input type="email" class="form-input" value="<%= user.email %>" placeholder="Nhập email">
            </div>
            
            <div class="form-group">
                <label>Số Điện Thoại</label>
                <input type="tel" class="form-input" value="0123456789" placeholder="Nhập số điện thoại">
            </div>
            
            <div class="form-group">
                <label>Ảnh Đại Diện</label>
                <div class="avatar-upload">
                    <img src="<%= user.avatar %>" alt="Avatar" class="avatar-preview">
                    <button class="btn btn-outline">Thay Đổi Ảnh</button>
                </div>
            </div>
            
            <button class="btn btn-primary">Lưu Thay Đổi</button>
        </div>
    </div>

    <!-- Security Settings -->
    <div class="settings-section">
        <div class="section-title">
            <span class="title-icon">🔒</span>
            <h2>Bảo Mật</h2>
        </div>
        
        <div class="settings-card">
            <div class="form-group">
                <label>Mật Khẩu Hiện Tại</label>
                <input type="password" class="form-input" placeholder="Nhập mật khẩu hiện tại">
            </div>
            
            <div class="form-group">
                <label>Mật Khẩu Mới</label>
                <input type="password" class="form-input" placeholder="Nhập mật khẩu mới">
            </div>
            
            <div class="form-group">
                <label>Xác Nhận Mật Khẩu</label>
                <input type="password" class="form-input" placeholder="Nhập lại mật khẩu mới">
            </div>
            
            <button class="btn btn-primary">Đổi Mật Khẩu</button>
        </div>
    </div>

    <!-- Notification Settings -->
    <div class="settings-section">
        <div class="section-title">
            <span class="title-icon">🔔</span>
            <h2>Thông Báo</h2>
        </div>
        
        <div class="settings-card">
            <div class="setting-item">
                <div class="setting-info">
                    <h4>Thông báo Email</h4>
                    <p>Nhận thông báo qua email về tiến độ học tập của con</p>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox" checked>
                    <span class="toggle-slider"></span>
                </label>
            </div>
            
            <div class="setting-item">
                <div class="setting-info">
                    <h4>Báo cáo tuần</h4>
                    <p>Nhận báo cáo tổng hợp hàng tuần</p>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox" checked>
                    <span class="toggle-slider"></span>
                </label>
            </div>
            
            <div class="setting-item">
                <div class="setting-info">
                    <h4>Thông báo bài thi mới</h4>
                    <p>Được thông báo khi có bài thi mới</p>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox">
                    <span class="toggle-slider"></span>
                </label>
            </div>
            
            <div class="setting-item">
                <div class="setting-info">
                    <h4>Thông báo thành tích</h4>
                    <p>Được thông báo khi con đạt thành tích mới</p>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox" checked>
                    <span class="toggle-slider"></span>
                </label>
            </div>
        </div>
    </div>

    <!-- Children Management -->
    <div class="settings-section">
        <div class="section-title">
            <span class="title-icon">👶</span>
            <h2>Quản Lý Con</h2>
        </div>
        
        <div class="settings-card">
            <% children.forEach(child => { %>
            <div class="child-item">
                <img src="<%= child.avatar %>" alt="<%= child.name %>">
                <div class="child-info">
                    <h4><%= child.name %></h4>
                    <p>Lớp <%= child.grade %> • ID: <%= child.studentId %></p>
                </div>
                <div class="child-actions">
                    <button class="btn btn-outline btn-sm">Xem Chi Tiết</button>
                    <button class="btn btn-outline btn-sm">Cài Đặt</button>
                </div>
            </div>
            <% }); %>
            
            <button class="btn btn-primary" style="margin-top: 1rem;">
                <span>➕</span> Thêm Con
            </button>
        </div>
    </div>

    <!-- Privacy Settings -->
    <div class="settings-section">
        <div class="section-title">
            <span class="title-icon">🛡️</span>
            <h2>Quyền Riêng Tư</h2>
        </div>
        
        <div class="settings-card">
            <div class="setting-item">
                <div class="setting-info">
                    <h4>Hiển thị hồ sơ công khai</h4>
                    <p>Cho phép người khác xem hồ sơ của bạn</p>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox">
                    <span class="toggle-slider"></span>
                </label>
            </div>
            
            <div class="setting-item">
                <div class="setting-info">
                    <h4>Chia sẻ thành tích</h4>
                    <p>Cho phép chia sẻ thành tích của con với cộng đồng</p>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox">
                    <span class="toggle-slider"></span>
                </label>
            </div>
        </div>
    </div>

    <!-- Danger Zone -->
    <div class="settings-section">
        <div class="section-title danger">
            <span class="title-icon">⚠️</span>
            <h2>Vùng Nguy Hiểm</h2>
        </div>
        
        <div class="settings-card danger-zone">
            <div class="danger-item">
                <div>
                    <h4>Xóa Tài Khoản</h4>
                    <p>Xóa vĩnh viễn tài khoản và toàn bộ dữ liệu</p>
                </div>
                <button class="btn btn-danger">Xóa Tài Khoản</button>
            </div>
        </div>
    </div>
</div>

<style>
/* Page Header */
.page-header {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 2px solid rgba(102, 126, 234, 0.1);
}

.page-header h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
    color: var(--text-dark);
    font-weight: 700;
    background: linear-gradient(135deg, #667eea, #764ba2);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.page-header p {
    color: var(--text-light);
    font-size: 1rem;
}

/* Settings Container */
.settings-container {
    max-width: 800px;
    margin: 0 auto;
}

.settings-section {
    margin-bottom: 3rem;
}

.section-title {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
}

.section-title.danger {
    color: #ff6b6b;
}

.title-icon {
    font-size: 1.75rem;
}

.section-title h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-dark);
    margin: 0;
}

.section-title.danger h2 {
    color: #ff6b6b;
}

/* Settings Card */
.settings-card {
    background: white;
    border-radius: 16px;
    padding: 2rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.06);
    border: 1px solid rgba(0,0,0,0.04);
}

.settings-card.danger-zone {
    border-color: rgba(255, 107, 107, 0.3);
}

/* Form Groups */
.form-group {
    margin-bottom: 1.5rem;
}

.form-group label {
    display: block;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text-dark);
    margin-bottom: 0.5rem;
}

.form-input {
    width: 100%;
    padding: 12px 16px;
    border: 2px solid rgba(0,0,0,0.1);
    border-radius: 10px;
    font-size: 0.95rem;
    transition: all 0.3s ease;
}

.form-input:focus {
    outline: none;
    border-color: #667eea;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

/* Avatar Upload */
.avatar-upload {
    display: flex;
    align-items: center;
    gap: 1.5rem;
}

.avatar-preview {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid rgba(102, 126, 234, 0.2);
}

/* Setting Item */
.setting-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 0;
    border-bottom: 1px solid rgba(0,0,0,0.06);
}

.setting-item:last-child {
    border-bottom: none;
}

.setting-info h4 {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 0.25rem;
    color: var(--text-dark);
}

.setting-info p {
    font-size: 0.85rem;
    color: var(--text-light);
    margin: 0;
}

/* Toggle Switch */
.toggle-switch {
    position: relative;
    display: inline-block;
    width: 52px;
    height: 28px;
}

.toggle-switch input {
    opacity: 0;
    width: 0;
    height: 0;
}

.toggle-slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: 0.3s;
    border-radius: 28px;
}

.toggle-slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 4px;
    bottom: 4px;
    background-color: white;
    transition: 0.3s;
    border-radius: 50%;
}

.toggle-switch input:checked + .toggle-slider {
    background: linear-gradient(135deg, #667eea, #764ba2);
}

.toggle-switch input:checked + .toggle-slider:before {
    transform: translateX(24px);
}

/* Child Item */
.child-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem 0;
    border-bottom: 1px solid rgba(0,0,0,0.06);
}

.child-item:last-of-type {
    border-bottom: none;
}

.child-item img {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    object-fit: cover;
}

.child-info {
    flex: 1;
}

.child-info h4 {
    font-size: 1.05rem;
    font-weight: 700;
    margin-bottom: 0.25rem;
    color: var(--text-dark);
}

.child-info p {
    font-size: 0.85rem;
    color: var(--text-light);
    margin: 0;
}

.child-actions {
    display: flex;
    gap: 0.5rem;
}

/* Buttons */
.btn {
    padding: 10px 20px;
    border-radius: 10px;
    font-weight: 600;
    font-size: 0.9rem;
    text-decoration: none;
    display: inline-block;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
}

.btn-primary {
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(102, 126, 234, 0.4);
}

.btn-outline {
    background: transparent;
    border: 2px solid #667eea;
    color: #667eea;
}

.btn-outline:hover {
    background: rgba(102, 126, 234, 0.1);
}

.btn-sm {
    padding: 6px 12px;
    font-size: 0.8rem;
}

.btn-danger {
    background: #ff6b6b;
    color: white;
}

.btn-danger:hover {
    background: #ff5252;
    transform: translateY(-2px);
}

/* Danger Zone */
.danger-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.danger-item h4 {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.25rem;
    color: #ff6b6b;
}

.danger-item p {
    font-size: 0.85rem;
    color: var(--text-light);
    margin: 0;
}
</style>
