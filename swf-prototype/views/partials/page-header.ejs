<!-- Page Header Component -->
<div class="page-header">
    <h1><%= icon || '' %> <%= title %></h1>
    <% if (typeof description !== 'undefined' && description) { %>
    <p><%= description %></p>
    <% } %>
</div>
